import { ComingSoon } from '@/components/common';

const Strategy = () => {
  return (
    <ComingSoon
      title="Strategy & Portfolio Optimization"
      description="Portfolio optimization with constraints, allocation grid"
      phase="Phase 3"
    />
  );
};

export default Strategy;
