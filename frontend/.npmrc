# Only fail on high/critical vulnerabilities.
# The 12 moderate vulnerabilities are in ajv@6 (transitive dep of eslint v9 via
# @eslint/eslintrc). They require the $data option to be triggered — which eslint
# does not use. Unfixable until eslint migrates @eslint/eslintrc to ajv@8.
audit-level=high
